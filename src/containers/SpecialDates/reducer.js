// import { ACTION } from './actions'

var initialState = []

export function dates(state = initialState, action) {
  // switch (action.type) {
  //   case ACTION:
  //     return action.payload
  // }
  return state
}
